# Claude Delivery Examples

Claude delivery examples should show how a human-facing skill uses the facilitator protocol.

For protocol examples, use:

```text
fasia/facilitator/protocol/examples/
```

For Claude-specific walkthroughs, add examples here.

Keep the distinction clear:

```text
protocol examples = agent-agnostic expected shape
Claude examples   = delivery-specific interaction patterns
```
